{
  "name": "setupso-api",
  "version": "1.0.0",
  "description": "SetupSO MVP Online — API REST",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "migrate": "node -e \"const knex = require('knex'); const cfg = require('./knexfile'); const env = process.env.NODE_ENV || 'development'; knex(cfg[env]).migrate.latest().then(() => { console.log('Migrations OK'); process.exit(0); }).catch(e => { console.error(e); process.exit(1); })\"",
    "seed": "node seeds/createAdmin.js"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "knex": "^3.1.0",
    "pg": "^8.11.5",
    "better-sqlite3": "^9.6.0",
    "uuid": "^9.0.1",
    "express-rate-limit": "^7.3.1"
  },
  "engines": {
    "node": ">=20"
  }
}
